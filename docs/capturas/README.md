# docs/capturas
Esta carpeta debe contener capturas de pantalla como evidencia del funcionamiento:

- `swagger-ui.png`         — Interfaz Swagger UI con el endpoint POST /api/citas
- `request-valido.png`     — Request válido ejecutado desde Swagger (respuesta 202)
- `request-invalido.png`   — Request inválido ejecutado desde Swagger (respuesta 400)
- `rabbitmq-panel.png`     — Panel de administración RabbitMQ mostrando las colas
- `billing-queue.png`      — Cola billing.queue con mensajes
- `notifications-queue.png`— Cola notifications.queue con mensajes
- `analytics-queue.png`    — Cola analytics.queue con mensajes
- `auditoria-csv.png`      — Contenido del archivo auditoria-citas.csv generado
- `error-log.png`          — Contenido del archivo citas-rechazadas.log
